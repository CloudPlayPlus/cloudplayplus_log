name: cpp_log
description: "Shared native logging core (FFI) for CloudPlayPlus: a leveled, batched, pluggable-sink C++ logger that funnels native-origin logs into the same app.log as Dart-origin logs."
version: 0.0.1
publish_to: 'none'
homepage: www.cloudplayplus.com

environment:
  sdk: '>=3.9.2 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  # Used by lib/cpp_log.dart for Utf8 string marshaling across the FFI boundary.
  ffi: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  #
  # Please refer to README.md for a detailed explanation.
  plugin:
    platforms:
      windows:
        ffiPlugin: true
      # -------------------------------------------------------------------
      # The platforms below are BUILD GLUE ONLY — WRITE-ONLY / NOT yet
      # verified on the respective platforms (see task P6). Windows is the
      # validated path. Each entry has matching native build files:
      #   macos   -> macos/cpp_log.podspec
      #   ios     -> ios/cpp_log.podspec
      #   linux   -> linux/CMakeLists.txt
      #   android -> android/build.gradle + android/src/main/cpp/CMakeLists.txt
      # The Dart FFI bridge resolves libcpp_log.so on Android/Linux and uses
      # DynamicLibrary.process() on Apple; treat non-Windows as experimental.
      # -------------------------------------------------------------------
      macos:
        ffiPlugin: true
      ios:
        ffiPlugin: true
      linux:
        ffiPlugin: true
      android:
        ffiPlugin: true

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
